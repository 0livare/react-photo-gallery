{
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "cd lib && pnpm run build",
    "watch": "cd lib && pnpm run watch",
    "demo": "cd demo && pnpm run start",
    "pub": "cd lib && pnpm run pub"
  }
}
